@model IEnumerable<Artist>
<div class="container p-3"
     <div class="row pt-4"
          <div class=" col-6"
               <h2 class="text-primary"> Artist list</h2>
        </div>
        <div class="col-6 text-right">
            <a asp-controller="Artist" asp-action="Create" class="btn btn-primary">Create new Artist</a>
        </div>
    </div>
    <br />

@if (Model.Count() > 0)
{
    <table class="table table-bordered table-striped" style="width:100%">
    <thead>
    <tr>
    <th>
    Artist name
    </th>
    <th>
    Artist age
    </th>


    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
        <td width="30%">@item.ArtistName</td>
        <td width="30%">@item.ArtistAge</td>
        <td width="10%">
        <div class="col-6 text-right">
        <a asp-controller="Artist" asp-action="Delete" asp-route-Id="@item.Id" class="btn btn-danger mx-1">Delete</a>
        <a asp-controller="Artist" asp-action="Update" asp-route-Id="@item.Id" class="btn btn-primary mx-1">Update</a>
        </div>
        </td>
        </tr>
    }
    </tbody>
    </table>
}
else
{
    <p>No items created yet</p>
}

</div>